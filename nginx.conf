server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Opcional: proxy API (editar BASE_PATH si hace falta)
  # location /api/ {
  #   proxy_pass http://host.docker.internal:8000/api/;
  # }
}